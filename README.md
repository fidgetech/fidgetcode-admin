# Fidgetcode Admin

These are interactive scripts that ask for needed info.

- `npm run createuser`
- `npm run createtrack`
- `npm run assigntrack`

To run with Firebase emulators, add `-- --emulator`
